// apps/svg-pixel-pattern-generator/app.config.js

import { appMeta } from './../registry.js';

export const app = appMeta('svg-pixel-pattern-generator');

export const aufbau = {
  elements : { mode: 'auto' },
};
