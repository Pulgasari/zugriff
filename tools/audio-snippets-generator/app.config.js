// tools/audio-snippets-generator/app.config.js

import { appMeta } from './../registry.js';

export const app = appMeta('audio-snippets-generator');

export const aufbau = {
  elements : { mode: 'auto' },
};
