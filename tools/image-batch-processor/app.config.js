// tools/image-batch-processor/app.config.js

import { appMeta } from './../registry.js';

export const app = appMeta('image-batch-processor');

export const aufbau = {
  elements : { mode: 'auto' },
};
